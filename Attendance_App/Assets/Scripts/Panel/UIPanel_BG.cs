using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UIPanel_BG : UIPanel
{
    // 배경 화면 change

    //private Image img;

    //string path = "Textures/snoopy";
    //Texture2D sampleTexture;

    //private void Awake()
    //{
    //    img = this.GetComponent<Image>();

    //    sampleTexture = Resources.Load<Texture2D>(path);
    //    ChangeTexture(sampleTexture);
    //}

    //public void ChangeTexture(Texture2D texture)
    //{
    //    Sprite originSprite = img.sprite;
    //    Rect rect = new Rect(0, 0, texture.width, texture.height);
    //    Sprite sprite = Sprite.Create(texture, rect, originSprite.pivot);
    //    img.overrideSprite = sprite;
    //}
}
